version: "3.8"

services:
  server:
    build:
      context: .
      dockerfile: Dockerfile
    image: ghcr.io/abihf/vs-tunnel-server:latest
    volumes:
      - "/home/abi/vs-tunnel:/home/ubuntu"
    hostname: vshome
    dns: [192.168.100.13]
